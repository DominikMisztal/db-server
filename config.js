const config = {
  db: {
    /* don't expose password or any sensitive info, done only for demo */
    host: "db4free.net",
    user: "domimik123",
    password: "admin1234",
    database: "doktor",
  },
  listPerPage: 10,
};

module.exports = config;
